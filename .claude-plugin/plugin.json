{
  "name": "orchestrate",
  "version": "0.49.0",
  "description": "Lead-orchestrated multi-agent PR pipeline with a deterministic security floor and a cross-session resource registry. Ships the /orchestrate skill, the supporting slash commands, and the runtime scripts. The always-on floor stays a settings.json hook at a stable path (wired with consent by orchestrate-setup.py configure), never plugin-gated.",
  "author": {
    "name": "sydlexius"
  },
  "homepage": "https://github.com/sydlexius/cc-orchestrator",
  "repository": "https://github.com/sydlexius/cc-orchestrator",
  "keywords": ["orchestration", "multi-agent", "security-floor", "pull-request", "ci"]
}
